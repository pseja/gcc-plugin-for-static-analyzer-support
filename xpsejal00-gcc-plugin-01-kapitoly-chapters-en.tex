% For compilation piecewise (see projekt.tex), it is necessary to uncomment it and change
% \documentclass[../projekt.tex]{subfiles}
% \begin{document}

%=========================================================================

\chapter{Introduction}
\label{introduction}

% FIXME: introduction to the topic (formal verification, static analysis, predator, code listener, gcc plugins, ...)
With the ever-increasing complexity of software systems and the growing demand for high-quality and secure code, formal verification methods have become essential in some parts of the software development industry \cite{qualitxinc}.
Formal verification methods are a set of mathematical techniques used to prove the correctness of software systems, going beyond traditional testing and debugging methods, which only test a finite number of inputs, allowing potentially critical errors to go undetected \cite{galois}.
The implementation of such formal methods is not straightforward, as it requires a deep understanding of both the verified software and the application of formal methods, typically necessitating specialized domain knowledge.
Formal verification tools, such as Predator or Forester \cite{predator, forester}, utilize static analysis to verify code written by developers.
In Predator's case, this involves verifying sequential C programs for memory safety while operating with pointers and linked lists \cite{predator}.
Predator is built on top of the Code Listener infrastructure \cite{code_listener_web}, which provides an application programming interface (API) foundation for creating such static analyzers.

% FIXME: motivation (why the refactor and the need for a new plugin, ...)
However, the core of the Code Listener infrastructure was implemented nearly fifteen years ago as a part of Predator, which has led to certain challenges in continuing its development and maintenance in its current form.
Consequently, the codebase relies on outdated programming practices and lacks the expressiveness, safety, and efficiency offered by modern C++ standards.
Moreover, this legacy technical debt, combined with the complexity of the existing codebase, hinders further development and makes maintenance and adaptation to newer GCC versions more difficult, as well as limits the potential for reusability in other static analysis tools.

% FIXME: solution approach (modularity, new C++ version, json, visualization, ...)
To address these challenges, this bachelor's thesis focuses on refactoring the Code Listener infrastructure into a standalone, modern GCC plugin \cite{gcc}.
The primary objective is to design and implement a new architecture that serves as a robust alternative to the existing solution.
By leveraging the capabilities of modern C++ standards, targeting C++23, the new implementation aims to simplify the internal structure, unify the API, and ensure overall long-term maintainability.
Furthermore, the modernized structure will allow for easier integration of new functionality, specifically the support for data structure visualization and export to JSON format.

% FIXME: end of introduction (structure of the thesis)
The thesis is structured as follows. Chapter \ref{preliminaries} provides an overview of the necessary background information, % TODO: including static program analysis, GCC, GCC compilation process, GCC plugins, Predator, and the Code Listener infrastructure and modern C++ practices.

\chapter{Preliminaries} % TODO: Theoretical Background ?
\label{preliminaries}

To understand the work presented in this thesis, it is essential to have a foundational understanding of several key concepts described in this chapter.

\section{Static program analysis}
\label{static_program_analysis}

Static program analysis (expand later) % TODO: (static analysis or SPA in short) is a process of analyzing the correctness of computer programs without the need to execute them with specific inputs.
% TODO: expand later

\section{GCC}
\label{gcc}

% TODO: asap
GCC

\subsection{GCC Compilation Process}
\label{gcc_compilation_process}

% TODO: asap
GCC Compilation Process

\subsection{GCC plugins}
\label{gcc_plugins}

% TODO: asap
GCC plugins

\section{Predator}
\label{predator}

Predator (expand later)
% TODO: expand later

\section{Control Flow Graph}
\label{control_flow_graph}

Control Flow Graph

\section{Three Address Code}
\label{three_address_code}

Three Address Code

\section{The Code Listener Infrastructure}
\label{code_listener_infrastructure}

The Code Listener infrastructure was designed to help with the implementation of static analyzers for C programs.
The main purpose of the Code Listener is to communicate with GCC during the compilation process, this allows it to handle everything that GCC does while compiling a program \cite{code_listener_web}, 
    translate the internal representation of the program being compiled, normalize it, 
    and provide it to the static analyzer for further analysis in the form of an API called Code Storage 
    or optional outputs, more on them later in this section.
This is visualized in figure 
% TODO: \ref{c4_scd} on page \pageref{c4_scd}.

\begin{figure}[hbt]
	\centering
	\includegraphics[width=0.3\textwidth]{diagrams/CodeListener/c4_scd.png}
	\caption{Code Listener system context diagram}
	\label{c4_scd}
\end{figure}

The Code Listener infrastructure consists of several components visualized in figure 
% TODO: \ref{cl_infrastructure} on page \pageref{cl_infrastructure}
, that work together to achieve the aforementioned goals.

\textit{Adapters} are the boxes that are embedded into the code parsers (e.g., GCC or Sparse) and are responsible for translating the internal representation (IR) of the specific code parser into a normalized format, which provides a consistent way to represent the compiled program regardless of the code parser used.

The \textit{code parser interface}, which represents the API used for communication between GCC or other code parsers such as Sparse (boxes on the left) and Code Listener during the compilation process.
The API is based on callbacks which the adapters use to emit constructs of the intermediate code during traversal of the parsers§ internal data structures.
Behind it, there are the \textit{filters} and \textit{listeners}.

Filters are reponsible for making transformations of the intermediate code emitted by the adapters, such as normalization or optimization and emit it to the code storage.
The \textit{switch to if} filter is an example of such a filter, which transforms switch statements intermediate code \texttt{SWITCH} into a sequence of if-else intermediate code \texttt{COND} instructions.

In contrast to filters, listeners use the API only as an input, install handlers for specific callbacks and process the intermediate code inside them.
Some examples of listeners are the optional outputs, such as \textit{control flow graph (CFG) plotter}, debug location tracer, pretty printer for three address code or type dependency graph visualizer.

Finally, there is the \textit{code storage}, which builds the persistent object model of the intermediate code emitted by the adapters and transformed by the filters.
When the whole object is built, it can be queried by the static analyzer for further analysis 
% TODO: \cite{code_listener}.

\begin{figure}[hbt]
	\centering
	\includegraphics[width=1.0\textwidth]{diagrams/CodeListener/structure.png}
	\caption{Code Listener infrastructure}
    % FIXME: \footnote{Taken from \cite{code_listener_web}}
	\label{cl_infrastructure}
\end{figure}

This is what this thesis focuses on modernizing and refactoring into a standalone, modern GCC plugin.

\section{Modern C++ Practices}
\label{modern_cpp_practices}

% TODO: expand when writing the plugin
Modern C++ Practices

\chapter{Design}
\label{design}

% TODO: when done with code listener and gcc
design

\chapter{Implementation}
\label{implementation}

implementation

\chapter{Testing}
\label{testing}

testing

\chapter{Conclusion}
\label{conclusion}

conclusion

%=========================================================================

% For compilation piecewise (see projekt.tex), it is necessary to uncomment it
% \end{document}

